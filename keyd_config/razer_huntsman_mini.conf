[ids]
1532:0257:030ed405

[main]
include common

# esc to backtick since there is no dedicated `/~ key
# I use alt by itself for escape anyways
esc = `

rightalt = layer(meta)

leftalt = overload(meta, esc)

leftmeta = layer(alt)

# Rightalt+space to alt+tab
# (currently only swaps between 2 most recent windows)
[meta]
space = A-tab
